{
  "name": "antigravity-os-mcp",
  "version": "2.0.0",
  "private": true,
  "description": "Antigravity OS v2.0 - MCP Server System with Temporal Memory and Intelligence Layer",
  "workspaces": [
    "packages/shared",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/build",
    "dev": "npm run dev --workspaces",
    "test": "npm run test --workspaces --if-present"
  }
}